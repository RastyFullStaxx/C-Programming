#include <stdio.h>

int main(){

    // augmented assignment operators = used to replace a statement where an
    //                                  operator takes a variable as one of its
    //                                  arguments and then assigns the result back
    //                                  to the same variables
    //                                  x = x + 1
    //                                  x += 1

    // +=           ADD
    // -=           SUBTRACT
    // /=           DIVIDE
    // %=           MODULUS

    return 0;
}